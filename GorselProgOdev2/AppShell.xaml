<?xml version="1.0" encoding="utf-8" ?>
<Shell 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:GorselProgOdev2.Pages"
    x:Class="GorselProgOdev2.AppShell">

    <TabBar>

        <ShellContent
            Title="Ana Sayfa"
            ContentTemplate="{DataTemplate pages:HomePage}" />

        <ShellContent
            Title="Kredi"
            ContentTemplate="{DataTemplate pages:CreditPage}" />

        <ShellContent
            Title="VKİ"
            ContentTemplate="{DataTemplate pages:BmiPage}" />

        <ShellContent
            Title="Renk"
            ContentTemplate="{DataTemplate pages:ColorPage}" />

    </TabBar>

</Shell>
